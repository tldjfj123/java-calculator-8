package calculator;

public class StringCalculator {
    public static int add(String text) {
        if (text == null || text.isEmpty()) {
            return 0;
        }
        // 아직 다른 기능은 구현되지 않았습니다.
        return 0;
    }
}